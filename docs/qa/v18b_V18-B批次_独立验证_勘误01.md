# 勘误 01 —— #64 报告 §4 表格 ⑨ 「段③写点数 9」应为 **6**

*编制：quality-lead-4（独立第三方验证）· 2026-09-16 · 关联报告：`docs/qa/v18b_V18-B批次_独立验证_20260916.md`（SHA256 `BA7CE2D9C1D3776D0807DDAF38ED0A6B5D9BE1A32B1A07B44B31BF611E9F2474`）*

## 1. 勘误内容（单点）

- **原文**：§4.1 表格第 ⑨ 行 —— 「段③写段（0 处过锁） | `:598-627` | **9 处** `fleet.stance = …` 直写；窗口内 `isStanceLocked` 计数 = 0」。
- **更正**：段③（`BattleScene.ts` 终态 `BF3A1E61…`）内 `fleet.stance =` **真实写点 = 6 处**，位于合并的 `if / else-if / else` 链（`:596-622`）：

  | # | 行 | 逐字 |
  |---|---|---|
  | 1 | `:598` | `fleet.stance = hpPct > (personalityType === 'aggressive' ? 0.3 : 0.5) ? 'siege' : 'fallback';` |
  | 2 | `:600` | `fleet.stance = 'fallback';` |
  | 3 | `:602` | `fleet.stance = 'siege';` |
  | 4 | `:604` | `fleet.stance = 'search';` |
  | 5 | `:606` | `fleet.stance = 'search';` |
  | 6 | `:621` | `fleet.stance = 'search';` |

- **不变项**：段③窗口内 `isStanceLocked` 出现次数 **= 0**（未过锁）——**原判定正确，维持不变**。

## 2. 根因（原报告「9」从何而来）

原探针 `_qa58_item4_allylock.cjs` 第 100 行的统计正则为 `win.match(/fleet\.stance\s*=/g)`——**无 `(?!=)` 前瞻**，故 `fleet.stance === 'siege'`（`===` 以 `=` 起始）**亦被计入**。窗口 [过滤 anchor → 扩张段头] 内该正则命中：

- 6 处**真写点**（上表）；
- 3 处**比较读**：`:625` `fleet.stance === 'siege'`、`:626` `fleet.stance === 'siege'`、`:627` `fleet.stance === 'fallback'`。

⇒ `6 + 3 = 9`。报告 §4 表格把该探针读数（9）**误作纯写点数**引述。

用带负前瞻的正则 `\b(fleet|fl)\.stance\s*=(?!=)` 重算 ⇒ **6**（本次复核已逐行枚举落盘）。

## 3. 影响面评估

- **对 #64 判定**：**无影响**。原探针断言 P2k 为阈值型（`>= 5`）而非精确值，故 9 或 6 均 PASS；CONCERN 的成立要件是「段③对『可被锁舰队』存在**无条件** stance 写点 + 窗口内 `isStanceLocked` **= 0**」——这两点逐字复核后**依然成立**（写点 6 处全部无条件）。**#64 判定的 PASS + 1 CONCERN（CONCERN-CONFIRMED）维持不变。**
- **对 #67（盟友类 triage）**：**有影响——修复范围口径**。任务单把选项 (a) 描述为「段③ **9 写点**加锁门」；实际需加锁门的是 **6 个写点**（同一条 if/else-if 链内）。裁定与 hunk 方向请按 **6** 计。
- **性质**：属本报告 §5.3 已自列的「计数口径提醒」同类（正则口径），非新增缺陷、非本批引入。

## 4. 纪律说明

- 因原报告 SHA256（`BA7CE2D9…`）已被 #67 任务单引用为输入锚点，**未就地改写**原文件以避免指纹漂移；改用本**勘误单**置前更正。
- 本勘误不影响任何源码、不改动原探针产物；新增证据 `_qa67_enum.txt`（逐行枚举）、`_qa67_seg3_full.txt`（段③全文）。判据一律来自 `BattleScene.ts` 终态 `BF3A1E61…` 逐字抽取。
