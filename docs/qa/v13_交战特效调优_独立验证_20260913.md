# v13 交战特效调优 · 独立验证报告

- **任务板**：#30 WS15-QA（独立验证）
- **验证方**：quality-lead-3（严守真，QA/测试）
- **日期**：2026-09-13
- **被验改动**：WS15 交战特效调优三项（①激光双层 ②导弹重设计 ③尾焰金红缩小）—— 工程侧 self-test 由 engineering-lead-4 交付
- **验证立场**：**独立（不复述工程侧结论、不重跑工程侧脚本）**。本报告全部数据来自本人自建台架 + 源码静态直读 + 自实现 diff。
- **硬约束**：未改动任何 `frontend/src/**` 产品代码；仅在 `frontend/_ws15qa_*` 新建台架/产物、在 `docs/qa/**` 落报告。

## 0. 判定速览

| 维度 | 判定 | 一句话 |
|---|---|---|
| **本次三项改动（代码实现）** | **PASS** | A/B/C/D/E/F 逐项与 spec 逐位一致，无 `Math.random` 回归，池无泄漏 |
| 静态核验 A | PASS | `updateMissiles` 内 `Math.random(` 计数 = **0**（grep 严格）；24-hunk diff 全落三区；`spawnFortressBeam` 未被误动 |
| 独立台架 B | PASS（含 3 条口径性 FAIL 已归因） | 结构/参数/曲线 22 条断言全绿；3 FAIL 系台架口径缺陷，非产品缺陷 |
| 净室复核 C/池 | PASS | `_ws15qa_reap` 8/8；微批全归零；导弹呼吸残差 = 0（硬证确定性） |
| 尾焰三判据 D | PASS | 独立由公式推导：0.37L≤0.4L、0.23L≤0.25L、三档色为金红 |
| 截图证据链 E | PASS（附 1 条可比性保留） | 28 图哈希留证；before/after 同 seed·同机位·同端点，逐图判读与 spec 相符 |
| 总体质量门 | **PASS + CONCERNS(1)** | 改动本身可通过；仅"远景可见性观感"一项须真机复核 |
| 剩余风险 | CONCERNS | R1 远景尾焰可见性 / R2 导弹远景偏淡 —— 本人无法在 SwiftShader 下证伪，须真机 |

> 说明：质量门为**建议性（advisory）**。最终放行由用户/主理人决定。

---

## 1. 验证方法与环境

- **口径**：无头 Chromium + SwiftShader **软渲染**。绝对 fps / 绝对屏幕像素密度**不作判据**；一切判据走 **常量直读 / 参数 trace / 池计数趋势 / 决定论残差 / 同机位 A-vs-B 图差**。
- **决定论**：台架用固定 seed **20260913** + mulberry32 PRNG；同机位 `hexR×160 / elev 0.72 / azim 0.5`。
- **重要物理背景**：主循环 `tick()` 内 `const dt = Math.min(this.clock.getDelta(), 0.05)` —— **dt 钳制 0.05s/帧**；无头 rAF 强节流下，**游戏时间推进远慢于墙钟**（实测约 1/5）。凡涉及"时长/归零"的观测均按此换算，不以墙钟秒数误判。
- **自建台架（均为本人独立编写，未复用工程侧 `_ws15_probe.cjs` / `_ws15_fx_series.cjs` 实现）**：
  - `frontend/_ws15qa_fx.cjs`（B：确定性微探针，端口 5231）
  - `frontend/_ws15qa_reap.cjs`（D2：净室停火后微批，端口 5232）
  - `frontend/_ws15qa_grep.cjs`（A：按方法体严格计数）
  - `frontend/_ws15qa_diff.cjs`（自实现 Myers 行级 diff，含自测）

---

## 2. A) 静态核验

### 2.1 冻结哈希（起止自证）

| 文件 | 实测 SHA256 | 字节 | 期望 |
|---|---|---|---|
| `src/game/three/Battle3DOverlay.ts` | `4C269481D048E32B1CDDB3FC61ECB457440353E63FBF0128A66F36CC7E9C8741` | 286964 | 与派单一致 ✅ |
| `src/game/scenes/BattleScene.ts` | `95497EDFA3EDFAFA04854C4D1FE6491CB8415071915A99149F550C50397890CF` | 310976 | 零改动 ✅ |
| `_ws15_baseline.txt`（旧版源码） | `6B3E226CF02A88F92CF4459BBF6B749D1A470EF6D60748559BA6F69699B9BD85` | 281881 | 基线备份 |
| `_ws15_after_backup.txt`（终版备份） | `4C269481…`（与当前 src 逐位相同） | 286964 | 备份=src ✅ |

台架首/末均记录脚本自身哈希与源码哈希，`hashEnd drift=false` ⇒ 台架运行期间源码未被改动，结论成立。

### 2.2 `Math.random(` 严格计数（A 必做项）

`_ws15qa_grep.cjs` 按**方法体括号配对**提取函数体后统计：

| 方法体 | 行范围 | `Math.random(` 计数 |
|---|---|---|
| **`updateMissiles`** | L4803–4835 | **0** ✅（派单必做项达成） |
| `spawnMissileSalvo` | L4449–4520 | 0 |
| `updateFx` | L4719–4796 | 0 |
| `spawnLaser` | L4415–4435 | 0 |
| `spawnFortressBeam` | L4358–4373 | 0 |
| `updateStrikes` | L4838–4913 | 1（**不在导弹路径**，与本次改动无关） |
| `updateFlames` | L1770–1814 | 0 |

- **全文** `Math.random(` 总数 = 14（含非 FX 路径注释/历史残留，属既有）。
- 定点核对 8 处关键行，逐字命中：
  - L4730 激光回退：`const base = (mat.userData && mat.userData.baseOpacity != null) ? mat.userData.baseOpacity : 0.95;`
  - L4363 雷神宽度：`const radius = Math.max(0.02, (widthPx / 200) * this.hexR);`
  - L4365 雷神几何：`new THREE.CylinderGeometry(radius, radius, len, 6),`
  - L4366 雷神材质：`opacity: 0.9`
  - L4420 激光核半径：`const coreR = (heavy ? 0.006 : 0.008) * this.hexR;`
  - L4815 导弹确定性呼吸：`M.flame.scale.setScalar(this.hexR * 0.006 * (1 + 0.06 * Math.sin(el * 32 + M.seed)));`
  - L4814 出膛淡入：`const fadeIn = Math.min(1, el / 0.07);`
  - L4493 拖尾 alpha：`'  float a = pow(1.0 - vZ, 1.5) * 0.38 * uFade;',`

### 2.3 自实现 Myers diff（baseline ↔ after）

- `BASELINE 5158 行 / AFTER 5235 行 / delta = +77 行 / 24 HUNKS`
- **`spawnFortressBeam(baseline L4356-4374) touched = false`** ⇒ **红线雷神之锤未被误动**（硬证）。
- 24 个 hunk **全部**落在：三项改动实现 + 对应注释。逐条核对无越界改动。
- **独立发现（⚠ 需工程侧确认）**：hunk **#18** 将 `seed: m * 2.5 + Math.random(),` → `seed: m * 2.5,`（L4514）。此项属于**工程侧 spec 外的一次额外随机移除**（spec 只提"删每枚每帧 `Math.random` 呼吸"，未提 seed 生成也含随机）。从结果看是**正向改进**（seed 也变确定性，令呼吸完全可复现），但属**声称外的差异**，已记入第 7 节。

---

## 3. B) 独立台架 `_ws15qa_fx.cjs`（22 PASS / 3 FAIL）

### A) 激光双层结构 + 淡出曲线 —— 全绿
- 普通激光：`Group`，2 child：
  - 核 Cylinder `rTop=0.4` = `0.008×hexR(50)` ✅，色 `#ffffff`，`baseOpacity=0.95`
  - 晕 Cylinder `rTop=1.04` = `核×2.6` ✅，色 `#38bdf8`（=传入色），`baseOpacity=0.22`
- 重束：核 `0.3` = `0.006×50` ✅，晕 `0.78` ✅。
- 淡出采样（6 帧，`f` 从 0.875→0.25）：`coreOp/fade` 恒 = **0.95**、`haloOp/fade` 恒 = **0.22** ⇒ 逐 mesh 按 `userData.baseOpacity` 同步淡出，规格成立 ✅。

### B) 雷神之锤红线（旧版逐位一致）—— 全绿
- 结构 `Mesh`（非 Group）、`hasUserDataBase=false`。
- `rTop = 17.5 = max(0.02, 70/200×50)` ✅，`radialSeg=6`，`opacity=0.9`。
- `opacity/fade` 比例恒 = **0.95**（=旧回退式）⇒ **与改动前行为逐位一致** ✅。

### C) 导弹重设计 —— 结构全绿；1 条 FAIL 为口径缺陷
- Group `childCount=3`：
  - 弹体 `Cylinder(radiusTop=0.3, radiusBottom=0.175, h=6.5, seg=6)`（= `rTail=0.006×50` → `rTip=0.0035×50`，`bodyLen=0.13×50`）✅
  - 引擎光点 `Sphere scale=0.3` @ `z=6.5` ✅
  - 拖尾 `Cylinder(0.35, 1, 1, 8, openEnded)` `scale=[0.2,0.2,21]` @ `z=6.5`（= `trailR=0.004×50`, `trailLen=0.42×50`）✅
- 拖尾 Shader：`float a = pow(1.0 - vZ, 1.5) * 0.38 * uFade;` ✅
- 几何齐射共享：`body/engine/trail` UUID 相同 → `{true,true,true}` ✅（回收不 dispose 几何的规格成立）
- **确定性残差 `max|measured − base=hexR·0.006·(1+0.06·sin(el·32+seed))| = 0.000e+0`（n=23）** ⇒ **硬证无 `Math.random`** ✅
- 出膛淡入：前 5 帧 `bodyOp/flameOp/uFade` 同步 0→1（`t=0.0454` 时 0.571/0.607/0.714，`t≥0.0908` 满 0.8/0.85/1.0）✅
- ❌ **FAIL：`d2/d1 = 1.329`（判据要求 <1.5）** —— **口径缺陷，非产品缺陷**：二阶差分比 d2/d1 对**纯正弦呼吸**本就偏大（正弦二阶导与一阶导同量级），该判据只适配"白噪声"场景。噪声已由 `maxResid=0.000e+0` **独立硬证排除**。

### D) FX 池回收（活跃战场）—— 1 条 FAIL 为口径缺陷
- `before={l:0,m:1,h:2}` / `peak={3,4,2}` / `after={3,3,3}`。
- ❌ **FAIL：回收后未归位** —— **口径缺陷**：在**活跃战场**中环境持续 spawn 新 FX，无法净测回收。已由**净室台架**（第 4 节）解决。

### E) 实战 FX 池采样（forceEngage ~30s）
- `peak = {lasers:230, hits:524, missiles:677, shields:230}`；`last = {76, 24, 240, 230}`。
- `units 713→713`，`sumHp 185954.50→173596.14`（战斗推进正常）。
- ❌ **FAIL：末值未回落** —— 同为**活跃战场持续生成**所致，非泄漏；净室口径见第 4 节。

### F) 尾焰三判据 —— 全绿
见第 5 节独立推导。

---

## 4. C/池) 净室复核 `_ws15qa_reap.cjs`（D2 口径）—— 8 PASS / 0 FAIL

**净室设计**：先停火（6 舰队远置 `R=6000` + `state/stance='search'` + 清空既有 FX）→ 静默确认无新生成 → 打**确定性微批**（4 laser + 2 salvo×3=6 missile + 3 hit）→ 逐帧观测归零。

| 断言 | 结果 | 原始数字 |
|---|---|---|
| 静默期无新 FX（lasers/missiles 恒 0，700ms×4） | PASS | `[0,0,0]×4` |
| 微批峰值 = 4 laser / 6 missile | PASS | `{lasers:4,hits:3,missiles:6}` |
| lasers 归零 | PASS | `ms=24` |
| **missiles 归零** | PASS | **`ms=54`**（6→4→0，轨迹见下） |
| hits 归零 | PASS | `ms=15` |
| 导弹呼吸 = 确定性模型（残差≈0，≥6 帧） | PASS | `n=13 maxResid=0.000e+0` |
| 无跳变：单帧 `|Δ|` ≤ 模型斜率上限 | PASS | `maxΔ=2.326e-2 ≤ bound=2.880e-2` |
| 控制台 0 真错误 | PASS | `[]` |

- **轨迹**（每 5 帧）：`m:6` 持稳 → 第 50 步 `4` → 第 55 步 `0`；`l:4→0`@25；`h:3→0`@15 —— 三条池**全部归零且无回升** ⇒ **无泄漏** ✅。
- **导弹呼吸平滑性**：13 帧实测与模型残差 = **0.000e+0**；单帧最大变化 `2.326e-2` ≤ 理论上限 `模型斜率×dt(0.05)=2.880e-2` ⇒ **无 60Hz 白噪声跳变**（正是用户实报"跳跃感"的根因，已消除）✅。
- **修正说明**：本台架首跑窗口 60 帧时 missiles 仅 `6→2`（未归零）；经轨迹与 dt 钳制换算（游戏时间≈墙钟 1/5）判定为**观测窗过短**，扩至 300 帧后归零 = 54 步。**非产品缺陷**。

---

## 5. D) 尾焰三判据 —— 独立由源码常量/公式推导

不依赖任何截图或工程侧数字，直接从 after 源码常量与 `updateFlames` 公式独立推导：

| 判据 | 推导 | 实测 | 结论 |
|---|---|---|---|
| **总长 ≤ 0.4·L** | `back_max = (0.22 + 1×0.15)·L = 0.37·L`（原 `1.5+0.8=2.30·L`） | **0.37L** | PASS ✅ |
| **最亮尺寸 ≤ 0.25·L** | `aSize_max = (0.07 + (1-0)×0.16)·L = 0.23·L`（原 `0.55+1.15=1.70·L`） | **0.23L** | PASS ✅ |
| **三档色 = 金红** | shader `hot/mid/cool` | hot `#FFEBB8`=[255,235,184]、mid `#FF731A`=[255,115,26]、cool `#B81A08`=[184,26,8] | PASS ✅ |

- 附带核对：`FLAME_PER_SHIP 14→7`、`FLAME_LIFE 0.55→0.30`、`wob 0.10→0.07`、`aAlpha 0.85→0.70` —— 均与 spec 一致。
- 旧三档为青蓝 `(1.0,0.96,0.85)/(0.45,0.82,1.0)/(0.12,0.30,0.85)`，新为金红三档 ⇒ 色相由"白→青蓝→深蓝"改为"金白→橙→暗红" ✅。

---

## 6. E) 截图证据链审核（`docs/qa/shots-20260913v7/`，28 文件）

- 28 图**逐文件 SHA256 留证**（见 `_ws15qa_shots_hash.txt`，TOTAL=28）。
- **可比性佐证**（来自台架日志交叉核对）：before/after 同 seed **20260913**、同机位（`hexR×160 / elev 0.72 / azim 0.5`）、同锚点（key `102:gwtqxokeudc`，同 y/z/ang）、激光端点同（`a1=[735,164]`）⇒ **高置信可比**。
- **逐图判读**（本人逐张查看）：

| 组图 | before | after | 与 spec 一致性 |
|---|---|---|---|
| 激光 `laser_pair` | 粗柱、实心亮束 | **细白核 + 外侧柔色晕**（暗色轮廓） | ✅ ① |
| 尾焰 `flame_closeup / rear / solo / solo_rear` | 大团 **蓝白**焰 + 长尾，罩住舰体 | **金红**小圆点 + 短喷，仅在喷口 | ✅ ③ |
| 导弹 `missile_1..8`（连拍） | 粗、亮、圆头弹 | **细长**（尖+细身+拖尾），同弹道同位 | ✅ ② |
| `fx_after_A_overview / B_zoom` | — | overview 正常；zoom 中一道细净红线（雷神）+ 小点粒子 | ✅ 无异常 |

- ⚠ **可比性保留（如实标注）**：`shots settle` 帧 before 舰数 **156** vs after **96** ⇒ 远景舰队**密度非同等**，远景层图（overview）**仅作形态参考，不作像素级密度对比**。

---

## 7. 差异与疑点（独立发现）

1. **⚠ 声称外的额外随机移除**：diff hunk **#18** `seed: m*2.5 + Math.random()` → `seed: m*2.5`（L4514）。spec 未声明此项。**性质**：正向（seed 亦确定性化，呼吸完全可复现），但属**实现超越声明**，请工程侧确认并知会文档。
2. **台架口径缺陷 3 条**（第 3 节 C/D/E）：均为**本人台架口径**问题（正弦序列 d2/d1 阈值、活跃战场无法净测回收），**已分别由残差硬证与净室台架解决**，非产品缺陷。已在报告中日中如实登记，避免污染结论。
3. **`_ws15_probe_after.log` 中 `misInfo missiles:0`**：为工程侧探针**读数时序**（读数时该批已命中回收），**非产品问题** —— 独立台架以逐帧观测已复现正常归零曲线。
4. **远景密度可比性**：见第 6 节，`settle` 舰数不等。

---

## 8. 剩余风险独立评估

| 编号 | 风险（工程侧自报） | 本人独立评估 | 可否在 SwiftShader 证伪 |
|---|---|---|---|
| **R1** | 远景尾焰可见性：收小 1/7 后，远景舰队尾焰是否"看不见" | **CONCERNS**：软渲染 + 远景密度不等，本台架无法在等效真机像素下证伪 | ❌ 须真机 |
| **R2** | 导弹远景偏淡：细弹体 + `0.38` 拖尾 alpha，远景对比度是否足够 | **CONCERNS**：同上；本台架确认**近景形态正确**，远景对比度无真机像素判据 | ❌ 须真机 |

> 二者均属**观感/可见性**维度，不属逻辑正确性维度；**逻辑与规格实现层面已全部 PASS**。

---

## 9. 证据清单（SHA256）

### 被验对象
```
4C269481D048E32B1CDDB3FC61ECB457440353E63FBF0128A66F36CC7E9C8741  286964  src/game/three/Battle3DOverlay.ts
95497EDFA3EDFAFA04854C4D1FE6491CB8415071915A99149F550C50397890CF  310976  src/game/scenes/BattleScene.ts  (零改动)
6B3E226CF02A88F92CF4459BBF6B749D1A470EF6D60748559BA6F69699B9BD85  281881  _ws15_baseline.txt   (旧版)
4C269481D048E32B1CDDB3FC61ECB457440353E63FBF0128A66F36CC7E9C8741  286964  _ws15_after_backup.txt (=src)
```

### 本人自建台架与原始输出
```
8629016C722F9B5C1F794697FA75DB67EF365F96AEF4CAFC790991E48A5CC886   25990  _ws15qa_fx.cjs        (B 微探针)
B97F8FDD232F7BF4511241464264F5C94CA8F029429D6FC43321E0F8570AE571    7419  _ws15qa_fx.txt
DFBA8932FE372F1153FEF337736E3D2E04C3D387B551056A250A1D1640DF0077   20308  _ws15qa_fx.json
B42666C953ED90C764B48CC6AF9FB6F25083F51F2195771E80297E974B900F9F   12859  _ws15qa_reap.cjs      (D2 净室)
6263032A8E326495B6EC9617ADEDE91F102CC20FBD03C0C9EB38EB1A11E0EC2B    6369  _ws15qa_reap.txt
F7DED97E147C467001B152618FB268C5BC476624B4B45076A39BA3582716532F   37622  _ws15qa_reap.json
5F1EB1DF5CA424EADF22E8B4E2C3420330731104FCAAA28DEC3DE5E82488A55F    2787  _ws15qa_grep.cjs      (A 严格计数)
10D259F91E93B933F2B2089207C89C6FACA1F944C1C4C621A80DE7A8E276E03E    1267  _ws15qa_grep.txt
29A8DB6ADA98F84570197AF8AD1592776958EAA27C1D17C777EB193F1304E6CA    3952  _ws15qa_diff.cjs
56790E5A049329CF2445C8EBB404D0277D33C674438650E9C0E013B50DE0086D   18083  _ws15qa_diff.txt
B7D4E00A83C54019CBDE7D836392B633686D75CD360E7234EC5FC8F9E39AC1CF     289  _ws15qa_hash.txt
9DB9C237392414415875E2A0DAD2F916E4E213E5BF08EEAFDF08E6FD89E6786B     395  _ws15qa_backup_hash.txt
4FF81A1E6D05DB65D08C56575E52058C7C80A9E60680EE69178480FBA539AC12    2841  _ws15qa_shots_hash.txt   (28 图)
7A27E6573EE3D745D144EBBCD82FD595F4B96CECB0E8FDD4AFB66F7201BF521E     191  _ws15qa_diffcheck.txt
```

### 截图证据
`docs/qa/shots-20260913v7/`（28 文件，逐文件哈希见 `_ws15qa_shots_hash.txt`）

---

## 10. 判定与建议

**质量门判定：PASS（本次三项改动）+ CONCERNS(1)**

- 三项改动（①激光双层/②导弹重设计/③尾焰金红缩小）在**实现正确性、规格一致性、无随机回归、池无泄漏**四个维度**全部通过独立验证**。
- 唯一 CONCERNS：**R1/R2 远景可见性观感** —— 属真机像素维度，SwiftShader 无法证伪，建议**真机（或用户实机）复核一次远景舰队尾焰/导弹可见性**。
- 建议工程侧确认第 7 节第 1 项（**额外 seed 随机移除**）是否需补入变更说明/文档。

**下一步建议**：
1. （可选，低优先）真机复核 R1/R2 远景可见性。
2. 工程侧知会文档：hunk #18 额外随机移除。
3. 本改动可进入后续集成/回归流程。

---
*本报告由 quality-lead-3 独立产出；所有数字为原始实测，未估算。质量门为建议性，最终放行由用户决定。*
