# #67 (a) 独立验证 —— 段③「1Hz 战略姿态评估」补 L1 锁门（D3 契约补全）

- **对象**：`frontend/src/game/scenes/BattleScene.ts`
  - 终态 `B3F1B307895CAF2597ABC8A84E4460909A123BA363DB3424E9B79ED66ECA0594` · 357738 B · 5813 行 · 全 CRLF
  - 前态基线 `BF3A1E61C98B0CF8EBB1D8D7C9161A8C03FEF8396CBF8CF6EADCCAA1D850239C` · 357004 B · 5808 行
  - Δ = **+734 B / +5 行**
- **实现方**：engineering-lead-6（回执 `_v67_receipt.txt`，hunk `_v67_hunks.txt`）
- **验证方**：quality-lead-4（独立第三方 · 自建 applier，**未重用** `_v67_reconstruct.cjs`）
- **依据**：design-strategist #67 裁定 §2（(a) = D3 契约补全）
- **判据来源**：冻结源码逐字抽取 + 自建探针（`_qa67_lockcase.cjs`）；**未采信任何声明值**
- **结论**：**PASS**

---

## 1. 判定摘要

| 组 | 内容 | 结果 |
|---|---|---|
| S | 冻结锚（终态/基线/hunk 对位） | 8/8 PASS |
| R | 归约锁（正/反向 byte-exact + 往返闭合） | 3/3 PASS |
| B | 对称边界（netBrace 一致） | 2/2 PASS |
| W | 结构最小性（仅空白 + 1 层包裹） | 6/6 PASS |
| C | **行为合同**（锁期不覆写 / 零回归 / 用例有牙 / 边界） | 7+7 PASS |
| X | 旁证（锁计数 / #63 清理未回退） | 3/3 PASS |
| — | `vue-tsc --noEmit` | **EXIT 0** |
| — | 同批其它 src 未动 | 4/4 **SAME** |
| **合计** | | **30/30 PASS** |

---

## 2. 冻结锚（S）

- `S0` 活体 sha256 == 声明终态 `B3F1B307…` ✓
- `S1` 备份 `frontend/_v67_backup/BattleScene.ts` sha256 == 前态基线 `BF3A1E61…` ✓（基线独立可证）
- `S2` 活体/备份**均全 CRLF**（无孤立 LF）✓ 、`S3` 行数 5813 / 5808 ✓
- `S4-S5` hunk 自解析：`---FINAL` 41 行 / `---BASE` 36 行（净 +5）✓
- `S6-S8` FINAL 逐字命中活体 `:592-632`、BASE 逐字命中备份 `:592-627`，起始行均为 592 ✓

## 3. 归约锁（R · 自建 applier）

- `R1` **反向归约**：活体 −FINAL +BASE → **byte-exact == `BF3A1E61…`（备份前态基线）** ✓
- `R2` **正向推进**：备份 +FINAL −BASE → **byte-exact == `B3F1B307…`（活体终态）** ✓
- `R3` 往返闭合 ✓

⇒ 「**除本 hunk 外整文件零改动**」被**整文件哈希**证明（非片段抽样）。

## 4. 对称边界（B · 防 F-3 复现）

- `B1` `netBrace(FINAL) == netBrace(BASE) == 0` —— 前后括号净差一致 ✓
- `B2` `netBrace(FINAL) = 0` —— 锁门 `if (...) {`（`:595`）与其闭合 `}`（`:632`）**自平衡**，未破坏外层 `for/forEach` 括号结构 ✓

## 5. 结构最小性（W）

- `W1` FINAL[5..40] 与 BASE **逐行去缩进后完全一致**（体仅空白变化）✓
- `W2` FINAL 体每行缩进 = BASE **+4 空格**（或空行）—— 纯重缩进 ✓
- `W3` 头 3 行为新增依据注释，含 `(a)` 标记 ✓
- `W4` FINAL[3] = `if (!this.isStanceLocked(fleet)) { // [V18-B/#67 · (a)] …` ✓
- `W5` 末行为闭合 `}`（缩进回 BASE 层）✓ 、`W6` BASE[0] 为旧锚注释 ✓

⇒ 采用 design-strategist **首选方案**（整块包锁门，`:592-632`），**一次齐过锁 11 处写点**（stance×6 / formation×4 / `_avoidUntil`×1）。

## 6. 行为合同（C · 逐字抽取段③体真跑）

段③体自源码**逐字抽取**（前态 `BASE` 体 / 终态被包裹 `FINAL[5..40]` 体），在 mock `this`（含 `time.now`）下真执行。

- `C3` **用例有牙**：前态体 + 锁期 → `hold`→`siege` **确实被覆写** ⇒ 本用例**能侦测缺陷**（非空断言）✓
- `C1` **终态体 + 锁期 → stance 保留 `hold`**（锁期不覆写，合同达成）✓
- `C1b` 锁期 → `formation`/`_avoidUntil` **亦不被写**（锁门覆盖全块）✓
- `C1c` 锁期 → `:600 fallback` 支不生效 ✓ 、`C1d` 锁期 → `:621 else` 支不生效（含 `_avoidUntil`）✓
- `C1e` **边界 `now == lockUntil` → 锁失效 → 正常覆写**（`>` 语义正确，非 `>=`）✓
- `C2` **零回归矩阵（7 场景）**：无锁舰队 `ref(前态) vs live(终态)` 的 `stance`/`formation`/`_avoidUntil` **逐位一致** ✓
  - 7 场景：`fr>2.0→siege`、`hp<0.35→fallback`、`else→search+avoid`、塔火+激进→siege、塔火+保守低血→fallback、`fr>1.2→search+line`、`fr>0.6→search`

⇒ **修复只在锁窗口内改变行为**；锁外（含全部敌军 team2）逐位不变。

## 7. 旁证与同批回归（X）

- `X1` 活体 `isStanceLocked` = 12（1 定义 + 10 调用 + 1 注释）✓
- `X2` 活体 `_userStanceLockUntil` = 2（1 写 1 读）✓
- `X3` 活体 `type === 'ally'` **活码命中 = 0**（全 5 处均在注释）—— #63 清理未回退 ✓
- **同批其它 src 指纹全部 SAME**：`App.vue` / `TacticalCommandSystem.ts` / `Battle3DOverlay.ts` / `types/game.ts` ⇒ 仅 `BattleScene.ts` 改动 ✓
- `vue-tsc --noEmit` = **EXIT 0**（无输出）✓

---

## 8. 对既有判定的影响

- **#64 §4 CONCERN（段③对「可被锁舰队」不设防）→ 已由 (a) 关闭（写者侧）**：段③ 6 处 stance 直写（连 formation×4 / `_avoidUntil`×1）现经 L1 锁门，与 L2×5 / L3×4 **同口径**。「可被锁 + 未过锁」反例**不再成立**。
- **#64 判定本身不变**：报告是对 `BF3A1E61…` 快照的判定（PASS + 1 CONCERN），历史快照保持不动；本单为**后续修复的收口验证**。
- **对 #67 (b)/(c) 无影响**：本 hunk **未触碰** `:543` 玩家闸 / `App.vue:35` UI 守卫 / `:2885` 选中判定 —— (b)/(c) 仍属**玩法变更**、待用户拍板，未擅自落地 ✓（与裁定 §5 一致）。

## 9. 台账口径小瑕疵（advisory · 非阻断 · 不影响行为）

1. 回执 §改动摘要写「等价于 **9** 处逐一加锁」，但**同行上句**已枚举 **11** 处（stance×6 + formation×4 + `_avoidUntil`×1）。「9」应为**早期口径残留**（修正前误计）；**行为无涉**，建议回执订正为 11。
2. 回执 §自验③ 写「`type==='ally'` 代码命中=0（**4** 命中全在注释）」——独立复算**全量 5 处**（`:525/:640/:832/:2885/:2890`，均在注释），**活码命中 0** 的实质结论**成立**；仅计数由 4 应订正为 5。

## 10. 下一步

- 落盘：本文件 + `_qa67_*` 证据（含可复跑探针 `_qa67_lockcase.cjs`）。
- **放行建议**：本单 **PASS** → 段③锁门可随 V18-B/#67 收口；**高影响动作（放行/发布签字）须用户人工审批**，本判定为 **advisory**。
- 回归资产：`_qa67_lockcase.cjs` 的 **C 组**即可作为「**僚舰锁期不覆写 stance**」常驻回归用例（与 L2×5 / L3×4 锁用例同组）；`C2` 矩阵即「**team2 无锁行为不变**」对账。

---

## 11. 追记 —— §9 两条 advisory 的订正复验（2026-09-16）

engineering-lead-6 已就 §9 两条口径瑕疵改**文档（src 零改动）**，本人**独立复算确认**：

| 项 | 复验结果 |
|---|---|
| `_v67_receipt.txt` | 独立重算 `10972CDF517517D451E2DE0B8BCBD696839046652759DDB97BC21016E8441706` / 2671 B **== 其声明** ✓；`:27` 已为「等价于 **11** 处逐一加锁」（9→11 已改）✓ |
| `_v67_hunk_ledger.txt` | 独立重算 `B325AADEC9EF459C932C9ECA4BC17EA7095EDFF4DD1E060CF5077E9E89DEE25B` / 5295 B **== 其声明** ✓；`:60-61` 已为「**5** 处命中全在注释（含 `:2885` 无空格变体…）」✓ |
| `BattleScene.ts`（src） | 独立重算 `B3F1B307…` / 357738 B **不变** ✓（订正确为纯文档） |

- 实现方自述订正根因：「4」系用带空格模式 `\.type === 'ally'` 统计，漏掉 `:2885` 的无空格 `type==='ally'` —— 与本人复算（全量 5 / 活码 0）一致。
- ⇒ **§9 两条 advisory 均已闭环**；本单 **PASS（30/30）** 判定不变，src 终态 `B3F1B307…` 不变。

---

*编制：quality-lead-4（独立第三方验证）· 2026-09-16 · 判据一律来自冻结源码逐字抽取 + 自建探针；归约锁采用自建 applier，未重用实现方脚本。*
