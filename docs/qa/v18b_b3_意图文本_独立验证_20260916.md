# B3 意图文本「（途中接战）」后缀 · 第三终端态独立验证报告

- **单据**：#66（owner = quality-lead-4，排 #64 之后）
- **被验对象**：engineering-lead-7 交付的 #58 批次 B 组尾项 · D2-C「意图可见」→ `TacticalCommandSystem.ts`（TCS）单文件单 hunk
- **验证角色**：独立第三方 · **语义级** —— 不复用 el-7 台架断言原文，自写用例自成判据
- **基线（before · #60 终态）**：`0C187E3E71F74994F4A52AE9703010705DA7890540A6CAB98816B9358C3AC10C` / 8136 B（备份 `frontend/_v18b_b3_backup/TacticalCommandSystem.ts`）
- **终态（after）**：`0B08B759CCC7DA1D5FA61CC69746562C52F7CDE4702B2DABCEBBB7D5A9A5DF7D` / 8589 B / 193 行（`\n` split）/ 全 **LF** / 无 BOM
- **改动摘要**：`fleetIntentText` 友方 mission 分支由 1 行 `return fleet.mission.text;` 展开为 7 行块，新增「（途中接战）」后缀（触发：`state==='engaging' && mission.type!=='attack_fleet'`）
- **日期**：2026-09-16
- **纪律**：只读 src（TCS 亦未改）；**未碰** `_v18b_b3_*` 与 `_v18b_*`/`_v18bfix_*` 既有产物（只读）；无 git 写、无 exe、未跑 wails dev。自建输出 `_qa58b3_recon_v1.ts` 唯一命名；证据统一前缀 `_qa58b3_*`。

---

## 0. 结论摘要

| # | 核证项 | 判定 | 依据 |
|---|---|---|---|
| 1 | 冻结 sanity：TCS 终态/基线哈希字节重算 | **PASS** | `_qa58b3_frozen_recon` F.1–F.4 |
| 1b | 反向还原（自研 applier）→ 命中基线 | **PASS** | R.1–R.3（字节逐位一致，输出件 `_qa58b3_recon_v1.ts` = `0C187E3E…`/8136） |
| 1c | 正向回放 → 闭合终态 | **PASS** | F.6–F.8 |
| 2 | 语义独立重导（自写用例） | **PASS** | `_qa58b3_bench` 18/18 |
| 3 | 调用点普查 + arity=3 | **PASS** | `_qa58b3_callsite` C.1–C.6 |
| 4 | #60 敌方脱敏回归（独立重跑） | **PASS** | bench D / D2 / O |
| 5 | 边界：retreating 不触发 + 已知近似登记 | **PASS**（登记为可接受近似） | callsite B.1–B.2 + bench H |
| 6 | `vue-tsc --noEmit` | **PASS** | EXIT 0 |
| 7 | el-7 台架可重跑佐证（不单独采信） | 一致 | 复跑 9/9 GREEN、确定；其 out 逐位复现 |

> **总判定：PASS（无 CONCERN）。**
> 后缀触发四条件、`attack_fleet` 排除、敌方路径零改动、role 前缀不受影响 —— **逐条成立**；反向锁精确命中基线；调用点唯一、arity=3；#60 脱敏契约未回归；tsc 通过。
> 唯一登记项为**已知近似**（在目标点附近接战亦显后缀），el-7 已自陈、属设计「mission 情境文本」表述范围内，**非缺陷**。

---

## 1. 核验方法与纪律

1. **零信任**：不采信 el-7 声明哈希/用例，一律自算 + 自写探针；判定式与函数体**逐字从冻结 TCS 抽取**后在 mock 上下文执行（`_qa58b3_bench.cjs`）。
2. **还原锁自研**：不复用 el-7 的 `_v18b_b3_reconstruct.cjs`，另写 applier（`_qa58b3_frozen_recon.cjs`）做反向（TO→FROM）与正向（FROM→TO）双向闭合，并校验 `netBrace` 对称。
3. **用例自写**：至少含 f/e/g 类（无 mission 的 engaging / hold_point 的 engaging / role 前缀组合），另加 M1/M2（flaring/assembling 不触发）、N（全角括号 U+FF08/U+FF09）、O（opts 缺省 fail-safe）等 el-7 台架未覆盖项。
4. **佐证不采信**：el-7 台架仅作**可重跑佐证**（复跑一致），判据以我的 `_qa58b3_*` 为准。

---

## 2. 逐项核证

### 2.1 冻结 sanity + 自研还原锁（`_qa58b3_frozen_recon_out.txt` · 15/15 PASS）

| 断言 | 结果 |
|---|---|
| F.1 TCS 终态 `0B08B759…/8589` | PASS |
| F.2 备份 == 基线 `0C187E3E…/8136` | PASS |
| F.3 TCS 全 LF（CR=0）· F.4 无 BOM | PASS |
| H.1 hunk 数 = 1 · H.2 FROM(基线)=1 行 / TO(终态)=7 行 · H.3 netBrace 对称（0==0） | PASS |
| H.4 TO ∈ 当前 ×1 · H.5 FROM ∉ 当前 | PASS |
| **R.1–R.3 反向 → 基线 SHA/bytes 命中 + 与磁盘基线字节逐位一致**（输出 `_qa58b3_recon_v1.ts`，其 SHA256 恰 == 基线 `0C187E3E…`） | PASS |
| F.6–F.8 正向 → 终态 SHA + 与磁盘终态字节逐位一致（往返闭合） | PASS |

### 2.2 语义独立重导（`_qa58b3_bench_out.txt` · 18/18 PASS）

| 用例（自写） | 输入要点 | 期望 | 结果 |
|---|---|---|---|
| A | 友方 + capture_planet + engaging | `夺取 最近的中立星球（途中接战）` | PASS |
| B | 友方 + capture_planet + exploring | `夺取 最近的中立星球`（无后缀） | PASS |
| C | 友方 + **attack_fleet** + engaging | `攻击 比克古舰队`（**排除**） | PASS |
| D | 敌方 team=2 + mission + engaging（有目标名） | `攻击 比克古舰队`（**不含 mission 文本**） | PASS |
| D2 | 敌方 team=2 + mission + engaging（无目标名） | `交战中`（不含 mission 文本） | PASS |
| E | 友方 + hold_point + engaging | `坚守指定空域（途中接战）` | PASS |
| F | 友方 + **无 mission** + engaging | `攻击 比克古舰队`（走状态机，无后缀） | PASS |
| G | 友方 + capture_planet + engaging + **role=assault** | `[突击] 夺取 最近的中立星球（途中接战）`（role 保留） | PASS |
| H | 友方 + **retreat_supply** + state=**retreating** | `撤回后勤站补货`（**不触发**） | PASS |
| I | 友方 + support_fleet + engaging | `协同 友军舰队（途中接战）`（泛化） | PASS |
| M1/M2 | 友方 + mission + flaring / assembling | 原文本（无后缀） | PASS |
| N | 后缀 == 全角 `（途中接战）`（charCode 0xFF08/0xFF09） | PASS |
| O | **opts 缺省** → 视同敌方 | 不含 mission 文本（#60 fail-safe） | PASS |
| S1–S4 | 结构锚：后缀判定式逐字命中 / `enemy` 判定式未变 / role 块未变 / 友方分支已展开 | PASS |

**读判**：后缀触发**唯一条件**为 `state==='engaging' && mtype!=='attack_fleet'`；`attack_fleet` 的 engaging 即任务执行本身故排除；其余 mission 类型被接战抢占时加后缀（E/I 泛化）；role 前缀（后置拼接）不受影响（G）；敌方路径（`enemy===true`）完全不进该分支（D/D2/O）。

### 2.3 调用点普查 + arity（`_qa58b3_callsite_out.txt` · 8/8 PASS）

| 断言 | 结果 |
|---|---|
| C.1 全仓 `fleetIntentText` **实参调用点 = 1**（仅 `Battle3DOverlay.ts:3938`） | PASS |
| C.2 `B3D:3888` 已算 `team`（`fac.team ?? (fleet.factionId === 1 ? 1 : 2)`） | PASS |
| C.3 `B3D:3941` 传 `{ team }`（三实参） | PASS |
| C.4 `TCS:156` 定义三参、`opts?: { team?: number }` **无默认值** | PASS |
| C.6 `function.length === 3`（无 `=` 默认值 ⇒ 不被截断） | PASS |

### 2.4 #60 敌方脱敏回归 · 2.5 边界（同上探针）

- **#60 回归**：敌方（`team!==1`）路径在任何状态下均**不含 mission 文本**（D/D2 精确串断言；O 覆盖 opts 缺省 fail-safe）⇒ 脱敏契约未被 B3 破坏。
- **边界**：`B.1` 函数体**无**距离/到达判定（`Distance`/`_missionDest`/`Arrived` 均不出现）⇒ 「目标点附近接战也显后缀」为**改法固有近似**；`H` 佐证 `retreat_supply` 运行时 `state==='retreating'` 不触发。
- **登记**：该近似在 el-7 台账 §6 已自陈，属设计「mission 情境文本」表述范围，**登记为可接受近似，非缺陷**（如需区分"在途/已抵达"，须引入 `_missionDest` 距离判定，属功能扩展，另议）。

### 2.6 类型检查与佐证

- `node node_modules/vue-tsc/bin/vue-tsc.js --noEmit` → **EXIT 0（无输出）**（`_qa58b3_tsc_out.txt`）。
- **el-7 台架佐证**（非破坏复跑 `_qa58b3_el7bench.cjs`）：`PASS=9/9 · 跑两遍一致=YES · GREEN · arity=3`；其输出件逐位复现（`frontend/_qa58b3_el7bench_out.txt` = `39B744BB…`/1113，与 el-7 声明一致）。

---

## 3. 证据件（`_qa58b3_*` · SHA256 / 字节）

| 证据件 | SHA256 | B |
|---|---|---|
| `_qa58b3_frozen_recon.cjs` | `FB0498EDD217BA922E62A80252C22099647E969630A3DD75987D982AE67AD300` | 4754 |
| `_qa58b3_frozen_recon_out.txt` | `D7E0B9C38281B5EF98C0DA0732BC8B940AF5DBB11EBE1D63CEA45C1B85C32C36` | 1170 |
| `_qa58b3_recon_v1.ts`（反向产物，== 基线） | `0C187E3E71F74994F4A52AE9703010705DA7890540A6CAB98816B9358C3AC10C` | 8136 |
| `_qa58b3_bench.cjs` | `5A0AE67DDD100EC5956676B2578AF7E5D142D545B3A6B94A3E111F54805DCAF1` | 9969 |
| `_qa58b3_bench_out.txt` | `C788AE4E685BBF046BB85980C4A6BAD6C3E94AEBABB5D0388A3B902E4FD115CD` | 2559 |
| `_qa58b3_callsite.cjs` | `FCB7974D14BFB99CB68799DD0F91A60ADA54D044AB5E05EC0FD45DDC44FAC2F4` | 4568 |
| `_qa58b3_callsite_out.txt` | `7A801FC4212B0A9A341C90D8EDB1FC771AA5B89802B13EEB395EC003324B60C1` | 741 |
| `_qa58b3_tsc_out.txt` | `418A5C17F33C70E99B0CC0A07FCE69191489CFEDC94164BFA903785777C5BD4B` | 7（`EXIT=0`） |
| `_qa58b3_el7bench.cjs`（佐证副本） | `DE7AC954AF0BE9C31DE0501ECCC9D7EEC05FF8F902ECF0E5BFC47FCFB717C581` | 6850 |
| `frontend/_qa58b3_el7bench_out.txt`（佐证副本输出） | `39B744BB167A58B5E972B0AF8E4E62D3B479F6D24C81FEDED4F762E38C8E62BC` | 1113 |
| `_qa58b3_manifest.txt` | 见 `_qa58b3_manifest.txt`（运行期清单） | — |

> 复跑：`node _qa58b3_frozen_recon.cjs` / `node _qa58b3_bench.cjs` / `node _qa58b3_callsite.cjs`（均幂等）。

---

## 4. 遗留 / 需 team-lead 路由

1. **已知近似**（目标点附近接战亦显后缀）：非缺陷，登记备查；如需精确「在途/已抵达」区分，属功能扩展（需 `_missionDest` 距离判定）。
2. **冻结面**：B3 只碰 TCS；BS（`BF3A1E61…`）、B3D（`D2E113BE…`）、gameStore、CouncilWarRoom、types 均未动（哈希经 #66 冻结 sanity 与 #64 复核一致）。
3. **上游依赖**：#66 未发现 TCS 自身缺陷；若后续有人改动 B3D:3888 的 `team` 来源或新增 `fleetIntentText` 调用点，须回归本报告 §2.2 的 D/D2/O（脱敏）与 E/I/G（后缀）用例。

---

## 5. 下一步

- 报告与证据落盘（本文件 + `_qa58b3_*`），回传 team-lead。
- 无阻塞项；放行/发布签字为高影响动作，**须用户人工审批**；本报告判定为 **advisory**。

---

*编制：quality-lead-4（独立第三方验证）· 2026-09-16 · 判据一律来自冻结源码逐字抽取 + 自建探针，未采信任何声明值。*
