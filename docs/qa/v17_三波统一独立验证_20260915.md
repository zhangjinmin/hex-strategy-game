# V17 · R10 三波修改统一独立验证报告（第三方 / 第二层 QA）

- **任务**：#54 V17-QA（R10 第10轮真机反馈 + V17 第11轮，共三波：波1 #49 / 波2 #52 / 波3 #53）
- **验证人**：quality-lead-4（严守真 · QA 域）
- **验证口径**：**独立第三方**——所有判据均来自本人实读冻结源码 + 自建探针；实现方自证产物（`_r10b1_*` / `_v17b2_*` / `_v17r3_*`）仅作覆盖路径的只读参考，**未作任何判据**。
- **报告落盘**：2026-09-16 09:5x（任务派单文件名日期 20260915）
- **最终判定**：**PASS**（0 阻塞项；1 项咨询级 CONCERN：台账行号漂移；1 项信息级提示：信标视觉单槽共享）

---

## 一、验证环境

| 项 | 值 |
|---|---|
| 工作目录 | `F:\Github\game_maoxiao\hex-strategy-game` |
| 受管 node | `C:\Users\zhangjinmin\.workbuddy\binaries\node\versions\22.22.2-3\node.exe`（v22.22.2） |
| 类型检查链路 | `frontend/package.json` → `vue-tsc --noEmit`（vue-tsc ^1.8.27 / typescript ^4.6.4） |
| 源码读取 | 只读；探针经 `ts.transpileModule` 去类型后在 mock 上下文**真实执行**抽取代码 |
| shell | bash 已损坏（Exit 127）⇒ 全程 PowerShell + 输出重定向落盘；node 侧直接 `fs.writeFileSync(utf8)` 规避 UTF-16 陷阱 |
| git | 仅只读（`status` / `log` / `diff`）；未做任何写操作 |
| 未执行 | 未构建 exe、未跑 wails dev（真机由用户执行） |

**防重入扫描**：开工前 `Glob **/_qa17*` = **0 个**（主理人 09:31 扫描结果一致）⇒ 无并发实例，正常开工。全部新产物以 `_qa17` 前缀落项目根，未触碰任何 `_lead_* / _r10b1_* / _v17b2_* / _v17r3_*` 既有文件。

---

## 二、冻结基线（权威身份 = SHA256 + 字节数）

### 2.1 验证前（`_qa17_hashes_before.txt`）

| 文件 | SHA256 | bytes | ReadAllLines |
|---|---|---|---|
| `frontend/src/game/scenes/BattleScene.ts` | `1A675F90…4CE74` | 347301 | 5694 |
| `frontend/src/game/three/Battle3DOverlay.ts` | `E05FFFFC…4CBA2` | 299860 | 5411 |
| `frontend/src/game/SupplyChainSystem.ts` | `FB336980…79B5` | 14527 | 295 |
| `frontend/src/App.vue` | `C6A5A898…85C2` | 43896 | 789 |

### 2.2 验证后（`_qa17_hashes_after.txt`，全部探针跑完后复算）

四文件 SHA256 / 字节数 / 行数**与 2.1 逐位一致**，且与派单冻结基线 4/4 相符。

> **行数口径**：本报告引用行号均为 **ReadAllLines 口径（1-based）**。本仓库 `App.vue` 无末行换行 ⇒ `split('\n')` 比 ReadAllLines 少 1（788 vs 789）；其余三文件末行有换行 ⇒ 两者相等。已按要求注明。

> 台账 `docs/review/r10_batch_notes.md`（权威 v2）复算 = `9814520E…53C2` / 8773 B / 108 行，与主理人指定权威版本一致（仅作引用，不作判据）。

**判定：冻结 4/4 PASS（前后各一次，零漂移）。** 未在漂移目标上继续深验。

---

## 三、逐项判据与证据

### 项 1 — 哈希冻结 4/4
见 §二。**PASS**

### 项 2 — tsc 类型检查 = 0 错误
- 命令：`node node_modules/vue-tsc/bin/vue-tsc.js --noEmit`（cwd = `frontend/`）
- 原始输出：`_qa17_tsc_out.txt` = `EXITCODE=0`，**stdout/stderr 全空（0 条诊断）**。
- **PASS**

### 项 3 — #49 运动学动态复现（自建台架 `_qa17_probe_h49.cjs` → `_qa17_probe_h49_out.txt`）
台架**逐字从冻结源码抽取**常量与函数体（非复写）：`HEADING_TURN_RATE` 源码串 = `"Math.PI / 120"`（:76）、`rateLimitAngle` 函数体（:81-86，166 字符）、`SHIP_TURN_RATE` = `"Math.PI / 6"`（B3D :655）、`[R10-B1-BEGIN..END]` 平滑块（去标记体 383 字符，SHA256=`4312A1CA…F1373`）；并抽验 `rateLimitAngle ≡ approachAngle`（5 点抽样）PASS。

指标定义：`夹角 = |wrap(位移方向 − 舰首朝向)|` 峰值；`编队半径保持 = min(单位均半径 / FORM_R)`。

| 场景 | 夹角峰值 | 持续>15° | 编队半径最低 | 判据 |
|---|---|---|---|---|
| **终态** 90° 转向 | **0.00°** | 0.00s | 95.6% | ≤15° → **PASS** |
| **终态** 180° 掉头 | **0.00°** | 0.00s | **95.4%** | ≥85% → **PASS** |
| 对照（#49 前语义）90° | 89.50° | 2.50s | 70.7% | — |
| 对照（#49 前语义）180° | 179.50° | 5.50s | 2.9% | — |

对照侧的 89.50° / 2.9% 与台账「修改前约 89.5° / 4.6%」同量级吻合（编队布局用均匀圆周近似，数值非逐点复刻），证明**本台架对目标病理敏感、非空跑**。

**倍率同步**（dt=store.currentSpeedFactor，:3488；60fps 基准）：

| dt | 每帧角步(rad) | 每帧角步(°) | 每帧位移(px) | 角步/dt | 游戏时间角速度 |
|---|---|---|---|---|---|
| 0.5 | 0.01308997 | 0.7500 | 0.1500 | 0.02617994 | **90.0000 °/s** |
| 1 | 0.02617994 | 1.5000 | 0.3000 | 0.02617994 | **90.0000 °/s** |
| 3 | 0.07853982 | 4.5000 | 0.9000 | 0.02617994 | **90.0000 °/s** |

- 每帧角步 ∝ dt（`step/dt` 恒 = HEADING_TURN_RATE）→ PASS
- 游戏时间角速度恒 = 90°/s（与 dt 无关）→ PASS
- 位移同步 ∝ dt ⇒ 转向/位移同基准，倍率 ≠1 时**不产生新的相对错位** → PASS

**PASS**

### 项 4 — #49 语义隔离
**fAngle 即时权威未被污染（实读）**：`let fAngle = fleet.facingAngle || 0`（:4278）；`fleet.facingAngle = fAngle` 两处（**:4290 / :4294**，均非限速包裹）；平滑块仅驱动 `fleet.heading` 与 `fleet.facingSmooth`（:4308-4311），**不写 fAngle**。探针断言「无 `rateLimitAngle(fleet.facingAngle)` 污染」PASS。

**战斗语义读取点实读（均读即时 `facingAngle`，未受影响）**：
- 包抄 AI：`closestEnemyFleet.facingAngle`（:4084）
- **背刺判定**：`angleToAttacker - closestEnemyFleet.facingAngle`（:4582）
- 舰名文本反旋：`textObj.rotation = -fAngle`（:4547，即时）

**消费点扫描（BS 非注释代码行，`_qa17_probe_logic_out.txt`）**：`heading/facingSmooth` 命中 12 行——
- 写入：:4308/:4309/:4310/:4311（**全部在平滑块内**）
- 位移：:4366/:4367/:4370/:4371（`fleet.heading`）
- 阵位旋转：:4487/:4488 + 精灵：:4508（`fleet.facingSmooth`）
- :4880 `sp.rotation = aux.heading` —— 系**补给舰 `aux` 独立对象**的自身航向，与 `fleet.heading` 无关（regex 命中，人工复核为非问题）
- 断言「无战斗语义行（backstab/angleDiff/cpDmg/incomingMult/reflectRatio）消费 heading/facingSmooth」PASS

**6 乘区调用点实读（均在战斗结算块，未被三波触及）**：`getSpeedMultiplier` :4351、`getDamageMultiplier` :4601、`getMoraleModifier` :4603、`getIncomingMultiplier` :4620、`getReflectRatio` :4622（+ 地形 `getTerrainSpeedMul` :4355 / `getTerrainRangeMul` / `getTerrainDodgeMul` / `getTerrainDodgeMul` 邻域）。语义未变。

**3D 侧（B3D，`_qa17_scan_b3d_out.txt`，5/5 PASS）**：
- `SHIP_TURN_RATE` 单点定义（:655）；**代码使用仅 1 处**（:3433）且位于 `hasFacing ? targetYaw : approachAngle(…, SHIP_TURN_RATE*dt)` 的 **else 兜底分支**。
- **兜底可达性**：`hasFacing = typeof fa === 'number' && Number.isFinite(fa)`，`fa = fleet.facingSmooth ?? fleet.facingAngle`；BS 建队即初始化 `facingAngle`（:868 / :1005，2 处）+ `facingSmooth` 首帧 = fAngle ⇒ 恒为有限数 ⇒ `hasFacing` 恒 true ⇒ **兜底分支不可达**（与声明一致）。
- **facingSmooth 消费点 4 处实读**：:2652（标记底框 `markerAxes`）、:3233（哨兵朝向）、:3423（舰体权威朝向，:3433 兜底）、:3526（addMesh 初始朝向）；各处均为 `facingSmooth ?? facingAngle` 缺省回退，**3D 侧无二次限速**。

**PASS**

### 项 5 — #52 幽灵信标门控前移 · 四路径（`_qa17_probe_logic_out.txt`，真实执行抽取的 `deployFlare`）
抽取 provenance：`createPlayerFlareVisual` :1541（体 1862 字符）、`deployFlare` :1591（体 2105 字符）。

| 路径 | 断言 | 结果 |
|---|---|---|
| 正常（direct+频道可） | 视觉创建（_visualCalls=1）**且** 舰队响应（targetFlare 写入 + stance='search'）**且** 旧视觉被替换销毁 | PASS |
| silent 拒 | 未创建新视觉（=0）**且** 旧信标保留（同引用且未 destroyed）**且** 未改舰队命令 | PASS |
| 频道满拒（direct 但 canOrderInstantly=false） | 未创建新视觉 **且** 旧信标保留 **且** 未改舰队命令 | PASS |
| 无接收舰队拒 | 未创建新视觉 **且** 旧信标保留 **且** 给出 toast「附近无可接收信标的己方舰队，未投放信标。」 | PASS |

顺序复核：源码 `deployFlare` 内「选舰队 → 无接收者守卫(1601 return) → silent 拒(1612) / delayed(1616) / 频道满拒(1624) → 生效(1631 建视觉)」；`createPlayerFlareVisual` 仅在 delayed 与"生效"两处调用，**全部在门控之后**。**PASS**

### 项 6 — #53 右键 move · 四路径 × 双端 + Esc 等
**`orderFleetMove`（:1648，真实执行）**：

| 场景 | 断言 | 结果 |
|---|---|---|
| player | targetFlare 写入 + stance=search + **mission 已清空** + 视觉创建 | PASS |
| ally（β 第二道纵深） | 拒绝（不移动）+ 未建视觉 + toast 归属提示 | PASS |
| silent | 拒绝 + 未建视觉 | PASS |
| 频道满 | 拒绝 + 未建视觉 | PASS |
| delayed | 建视觉（保留语义）+ 未立即移动 | PASS |
| 无单位 | 拒绝 | PASS |

**`applyFleetSelectByPick`（:2771，真实执行）**：player→true / ally→true / enemy→清选中+false / null→清选中+false。全部 PASS。

**2D 右键盘四分支（:3366-3390，真实分支块执行）**：
1. 选中 player → 调 `orderFleetMove`，**不**调 deployFlare。PASS
2. 选中 ally → **不降级信标**（deploy=0 且 move=0）+ toast「盟军舰队仅接受军议任务指令，无直接指挥权。」+ 选中未清。PASS
3. 未选中 → 落回信标（deploy=1）。PASS
4. 选择失效（id 不存在）→ **清选中** + 落回信标。PASS

**2D 左键选中块（:3395-3399，真实执行）**：命中舰队 → 调 `applyFleetSelectByPick`；选目标态（cpCommandMode）跳过。PASS。

**3D 端（`handlePick`，B3D）**：左键分支（`button===0 && !deployPhase`）调 `applyFleetSelectByPick?.(selFid)`（:5050-5053）；右键经 `syncTileClick` **透传真实 button（0/2）**（:5145-5160）回到 BS `handleTileClick` 的 `pointer.button===2` 分支 ⇒ **3D 右键 move 与 2D 同分支**。选中描边高亮在标记循环内（:2674-2680，读 `battleScene.battleSelectedFleetId`，与候选脉动互斥）。PASS。

**Esc 分层（:440-448，真实语句块执行）**：
- cpCommandMode=true → 清 `commandBridge.selectedAbilityId` + `exitTargetSelect('esc')`，**不波及** `battleSelectedFleetId`。PASS
- cpCommandMode=false 且有选中 → 清选中 + toast「已取消选中」。PASS
- 两者皆无 → 无副作用。PASS

**右键原生菜单**：`this.input.mouse?.disableContextMenu()`（:422）实读存在。PASS。

**PASS**

### 项 7 — 未动文件核对
- `App.vue`：SHA256/字节数与基线一致（C6A5A898…/43896 B）。实读 :36-38 三按钮 `store.dispatchFleetCommand(fleet.id,'stance', …)`（索敌/攻坚/驻守）、:53 `<CouncilWarRoom v-if="isCommandBattle" />` —— 与清单一致。
- `SupplyChainSystem.ts`：SHA256/字节数与基线一致（FB336980…/14527 B）。
- **清单外文件是否被本批改动**：对 `frontend/src` 做 mtime 取证（`_qa17_mtimes.txt`）——**2026-09-16 当天仅 `BattleScene.ts`（07:11:43）与 `Battle3DOverlay.ts`（07:11:52）被写**，正是本批（波1/波3）范围；`App.vue`（09-15 07:17）、`SupplyChainSystem.ts`（09-15 22:36）写入时间**早于本批**，属既有工作流遗留，非本批所动。**无清单外文件在本批被改。**
- 说明：`git diff HEAD` 显示 33 个 `frontend/src` 文件相对 Sep-6 基线有差异，系 **WS1–V18 全历史未提交累积**，**无法**用于隔离本批范围；故本项以「4 哈希冻结 + mtime 取证」为准（见 §四-3）。

**PASS（附范围说明）**

### 项 8 — 战斗语义泄漏扫描 / hunk 落位
- `[R10-B1-BEGIN]`（:4306）与 `[R10-B1-END]`（:4312）**成对**；块内仅 3 条语句：`headingStep = HEADING_TURN_RATE*dt`、`fleet.heading = rateLimitAngle(...)`、`fleet.facingSmooth = rateLimitAngle(...)`。
- 三波 hunk 落位（实读）：波1 = :76/:81（常量与工具）+ :4306-4312（平滑块）+ :4366/:4370（位移）+ :4487/:4488/:4508（阵位/精灵）；波2 = :1541-1585（抽方法）+ :1591-1635（重排）；波3 = :198-201/:422/:439-449/:1648-1692/:2771-2785/:3366-3399 + B3D :2652/:3233/:3423/:3433/:3526/:5050-5053/:2674-2680。**均落在声明区域，无意外波及**（项4 消费点扫描 + 项7 mtime 取证交叉印证）。

**PASS**（见下 CONCERN-1 关于台账行号的说明）

---

## 四、对抗性 / 反证检查

1. **台架敏感性（反证空跑）**：`_qa17_probe_h49` 的"对照"模式复现了 #49 前病理（90° 峰值 89.50°、180° 半径 2.9%），与台账修改前量级吻合 ⇒ 台架能区分修复前后，终态 0.00°/95.4% 不是恒真输出。
2. **探针 spy 有效性**：正常路径 `_visualCalls=1` 而三条拒路径 `=0` ⇒ 视觉创建探针确有区分力；若门控仍在视觉创建之后（旧顺序），silent 路径将 `=1`，本探针**能**捕获 → 结论非假阴性。
3. **幻影残留防护**：每个场景重建全新 mock（不跨场景缓存集合/对象引用），退出某状态后重新读取字段，避免"已退出仍见旧值"误判。
4. **单槽位共享（信息级提示，非缺陷）**：`deployFlare` 与 `orderFleetMove` **共用同一 `playerFlare` 视觉槽**（:104 / :1672 / :1685）。正常路径下新视觉会销毁旧视觉（探针「旧视觉被替换销毁」PASS）。这是既有设计语义，非本批引入；多舰队连续投放时**仅保留最近一个信标视觉**，属预期行为，特此登记供知悉。
5. **`:4880 aux.heading`**（regex 命中项）：经人工实读，`aux` 为补给舰对象，其 `heading` 与 `fleet.heading` 无关，**非 #49 波及项**，已排除。

---

## 五、判定与阻塞项

**总判定：PASS**（质量门建议放行）。

| 项 | 判定 |
|---|---|
| 1 哈希冻结 4/4（前后） | PASS |
| 2 tsc = 0 错误 | PASS |
| 3 #49 运动学动态复现 | PASS |
| 4 #49 语义隔离 | PASS |
| 5 #52 四路径 | PASS |
| 6 #53 四路径 × 双端 + Esc | PASS |
| 7 未动文件核对 | PASS |
| 8 语义泄漏扫描 | PASS |

探针合计：`_qa17_probe_logic` **48/48 PASS**、`_qa17_scan_b3d` **5/5 PASS**、`_qa17_probe_h49` **判据全 PASS**。

### 阻塞项清单
**无。** 未发现 Blocker / Critical / Major / Minor 级缺陷。

### 咨询级 CONCERN
- **CONCERN-1（文档，非代码）**：派单/实现期台账给出的 #49 定位行号存在漂移——台账标注「平滑块 :4167-4173、fAngle 权威 :4139-4155」，**终态实读**为「平滑块 [R10-B1-BEGIN/END] :4306-4312、fAngle 权威 :4278-4294」（偏移约 +139 行；B3D 侧台账 :3227/:3417/:3427/:3520 实读为 :3233/:3423/:3433/:3526）。**因 BattleScene 哈希与冻结基线逐位相符，以终态实读行号为准**；此漂移不影响任何判据结论，仅建议后续文档引用改用实读行号或锚点符号（如 `[R10-B1-BEGIN]`）。

### 信息级提示
- **INFO-1**：信标/移动命令共用单 `playerFlare` 视觉槽（见 §四-4），行为符合既有设计。

---

## 六、证据文件索引（全部 `_qa17*`，落项目根）

| 文件 | 内容 |
|---|---|
| `_qa17_hashes_before.txt` / `_qa17_hashes_after.txt` | 基线哈希（前/后） |
| `_qa17_tsc_out.txt` | vue-tsc --noEmit 原始输出（EXITCODE=0） |
| `_qa17_probe_h49.cjs` / `_qa17_probe_h49_out.txt` | #49 运动学台架源码与输出 |
| `_qa17_probe_logic.cjs` / `_qa17_probe_logic_out.txt` | #52/#53 行为探针源码与输出（48/48） |
| `_qa17_scan_b3d.cjs` / `_qa17_scan_b3d_out.txt` | B3D facingSmooth/SHIP_TURN_RATE 扫描（5/5） |
| `_qa17_mtimes.txt` | frontend/src mtime 取证（项7） |
| `_qa17_git.txt` | git 只读取证（HEAD / status / numstat） |
| `_qa17_toolcheck.txt` / `_qa17_docsqa.txt` | 环境与目录核查 |

**质量门建议：PASS（advisory）——最终放行由用户/主理人决定。**
